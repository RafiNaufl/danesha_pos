# Database Connection (Supabase)
# Connect to Supabase via connection pooling
DATABASE_URL="postgresql://postgres.[USERNAME]:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database. Used for migrations
DIRECT_URL="postgresql://postgres.[USERNAME]:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:5432/postgres"

# Authentication (NextAuth.js / Auth.js)
# Generate a secret with `openssl rand -base64 32`
NEXTAUTH_SECRET="your-secure-random-secret-string"
AUTH_SECRET="your-secure-random-secret-string"

# URL Configuration
# On Vercel, this is automatically handled, but you can set it to your canonical URL
NEXTAUTH_URL="http://localhost:3000"
AUTH_URL="http://localhost:3000"

# Trust Host (Required for Vercel)
AUTH_TRUST_HOST="true"
